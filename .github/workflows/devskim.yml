# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: DevSkim

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
  schedule:
    - cron: '38 14 * * 2'

jobs:
  lint:
    name: DevSkim
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
    steps:
      - name: Checkout code
        uses: actions/checkout@1af3b93b6815bc44a9784bd300feb67ff0d1eeb3

      - name: Run DevSkim scanner
        uses: microsoft/DevSkim-Action@4b5047945a44163b94642a1cecc0d93a3f428cc6
        with:
          ignore-globs: "**/.git/**,**/*.md,**/docs/**,**/docs_ui/**,**/test/**,**/tests/**,**/scripts/**,**/sample.env*,**/docker-compose.yaml,**/nginx/**,**/apache/**,**/helm/**,**/kubernetes/**,**/iis/**"
          exclude-rules: DS162092,DS176209

      - name: Upload DevSkim scan results to GitHub Security tab
        if: always() && hashFiles('devskim-results.sarif') != ''
        continue-on-error: true
        uses: github/codeql-action/upload-sarif@ce729e4d353d580e6cacd6a8cf2921b72e5e310a
        with:
          sarif_file: devskim-results.sarif
