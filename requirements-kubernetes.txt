# Optional Kubernetes support.
# Note: kubernetes 35.x no longer caps urllib3<2.4.0 (it currently requires
# urllib3!=2.6.0,>=1.24.2). This avoids forcing older urllib3 versions just
# to enable Kubernetes support.
-r requirements.txt
kubernetes~=35.0
