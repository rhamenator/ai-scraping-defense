{{- if .Values.services.cloud_proxy.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "asd.fullname" . }}-cloud-proxy
  labels:
    app.kubernetes.io/name: {{ include "asd.fullname" . }}
    app.kubernetes.io/component: cloud-proxy
spec:
  replicas: {{ .Values.services.cloud_proxy.replicas | default 1 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "asd.fullname" . }}
      app.kubernetes.io/component: cloud-proxy
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "asd.fullname" . }}
        app.kubernetes.io/component: cloud-proxy
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets: {{ toYaml . | nindent 6 }}
      {{- end }}
      containers:
        - name: cloud-proxy
          image: {{ .Values.services.cloud_proxy.image | quote }}
          imagePullPolicy: {{ .Values.global.pullPolicy }}
          ports:
            - containerPort: {{ .Values.services.cloud_proxy.containerPort | default 8080 }}
          env: {{ toYaml .Values.services.cloud_proxy.env | nindent 12 }}
          resources: {{ toYaml .Values.services.cloud_proxy.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "asd.fullname" . }}-cloud-proxy
  labels:
    app.kubernetes.io/name: {{ include "asd.fullname" . }}
    app.kubernetes.io/component: cloud-proxy
spec:
  type: {{ .Values.services.cloud_proxy.service.type | default "ClusterIP" }}
  selector:
    app.kubernetes.io/name: {{ include "asd.fullname" . }}
    app.kubernetes.io/component: cloud-proxy
  ports:
    - name: http
      port: {{ .Values.services.cloud_proxy.service.port | default 80 }}
      targetPort: {{ .Values.services.cloud_proxy.containerPort | default 8080 }}
{{- end }}
