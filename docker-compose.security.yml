# Compatibility overlay retained for existing deployment commands.
# The base compose file now applies no-new-privileges and drops capabilities
# directly, so repeating those lists here creates invalid duplicate entries when
# Compose merges the files.

services: {}
