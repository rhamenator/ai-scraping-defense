# Optional performance tuning directives (http context).
# Mount to /etc/nginx/conf.d/99-performance.perf.conf to enable.

tcp_nopush on;
tcp_nodelay on;
keepalive_requests 100;

client_body_buffer_size 128k;
client_header_buffer_size 1k;
large_client_header_buffers 4 8k;
output_buffers 2 32k;
